import Context from '../engine/context';
import { GameObject } from '../engine/gameObject';

export default class Player extends GameObject {
  public setup(context: Context): void {
    super.setup(context);
  }

  public loop(context: Context): void {
    super.loop(context);
  }
}
